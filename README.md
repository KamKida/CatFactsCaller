# CatFactsCaller
PL: Prosta aplikacja konsolowa pobierająca fakty o kotach za pomocą API, wyświetlająca je w konsoli i zapisująca w pliku tekstowym. 

ENG A simple console application that fetches cat facts using an API, displays them in the console, and saves them to a text file.
